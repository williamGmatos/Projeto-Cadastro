function confirmar() {
    var senha = document.getElementById('senha')
    var confirmar = document.getElementById('conf-senha')
    
    var Senha = Number(senha.value)
    var Confirmar = Number(confirmar.value)
    var versenha = document.getElementById('Msenha')

    
}